# Production-like configuration with subtle vulnerabilities

# Database - subtle secret exposure
DB_PASS="prod_secret_2026"
DB_HOST="production-db.internal"

# API configuration - mixed HTTP/HTTPS
API_BASE="http://legacy-api.company.com"
API_KEY=pk_live_test_key_only

# Server config - dangerous bind
LISTEN_ADDRESS=0.0.0.0
PORT=443

# Security - relaxed CORS
CORS_ORIGIN=* 

# Logging - debug in prod
LOG_LEVEL=debug
VERBOSE=true