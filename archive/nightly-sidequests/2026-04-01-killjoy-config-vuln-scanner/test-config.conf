# Test configuration file for vulnerability scanner

# Database configuration (VULNERABLE - hardcoded secret)
database_password = "super_secret_password_123"
database_host = "localhost"
database_user = "admin"

# API configuration (VULNERABLE - HTTP)
api_endpoint = "http://api.example.com/v1"
api_key = "sk_live_1234567890"

# Server configuration (VULNERABLE - bind to 0.0.0.0)
server_bind = "0.0.0.0:8080"
server_debug = true

# File permissions (VULNERABLE - open permissions)
file_permission = 777
chmod_command = "chmod 777 /var/log/app.log"

# Security headers (SAFE)
allowed_origins = ["https://example.com"]
cors_enabled = true